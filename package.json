{
  "name": "argblock",
  "module": "src/argblock.ts",
  "type": "module",
  "version": "0.0.12",
  "main": "dist/argblock.js",
  "types": "dist/types/argblock.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "bun run build:js && bun run build:types",
    "build:js": "bun build ./src/argblock.ts --outdir ./dist",
    "build:types": "tsc --project tsconfig.types.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "license": "MIT"
}
